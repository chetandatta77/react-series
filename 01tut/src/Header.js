const Header = () => {
    const headerStyle = {
        backgroundColor: 'royal',
        color: '#fff'
    }
    return (
        <header style={headerStyle}>
            <h1>Grocerries List</h1>
        </header>
    )
}

export default Header;